/* =========================================================
   BOOK III - DYNAMIC CANDIDATE EVALUATION AND BALLOT TYPES

   Registers metadata-driven election research controls.
   Candidate rosters and selection limits remain survey
   configuration; no candidate, position, or question ID is
   embedded in application code.
========================================================= */

INSERT INTO question_types (
    type_code,
    type_name,
    description,
    category_group,
    renderer_component,
    editor_component,
    preview_component,
    response_data_type,
    supports_options,
    supports_validation,
    supports_default_value,
    supports_calculation,
    supports_logic,
    supports_media,
    supports_repeat,
    supports_matrix,
    supports_offline,
    allowed_validation_rules,
    allowed_option_settings,
    default_settings_json,
    renderer_metadata_json,
    display_order,
    icon_name,
    help_text,
    is_system_type,
    is_active
)
VALUES
(
    'CANDIDATE_EVALUATION',
    'Candidate Evaluation',
    'Evaluate awareness, satisfaction, and trust for every configured candidate using governed integer codes.',
    'Election Research',
    'CandidateEvaluationRenderer',
    'CandidateEvaluationEditor',
    'CandidateEvaluationPreview',
    'json',
    TRUE,
    TRUE,
    FALSE,
    FALSE,
    TRUE,
    FALSE,
    TRUE,
    TRUE,
    TRUE,
    '["required"]'::jsonb,
    '{"candidate_roster":true}'::jsonb,
    '{"coding":{"awareness":{"aware":1,"not_aware":2},"satisfaction":{"satisfied":1,"neutral":2,"not_satisfied":3},"trust":{"trust":1,"neutral":2,"distrust":3}},"not_aware_auto_value":2}'::jsonb,
    '{"answer_schema":"eios.candidate-evaluation.v1","candidate_source":"QUESTION_CHOICE_LIST"}'::jsonb,
    26,
    'UsersRound',
    'Use with an editable candidate roster. Not-aware responses automatically derive neutral satisfaction and trust values with an audit flag.',
    TRUE,
    TRUE
),
(
    'BALLOT_SELECTOR',
    'Ballot Selector',
    'Select candidates for a configured electoral position with metadata-driven limits and awareness filtering.',
    'Election Research',
    'BallotSelectorRenderer',
    'BallotSelectorEditor',
    'BallotSelectorPreview',
    'json',
    TRUE,
    TRUE,
    FALSE,
    FALSE,
    TRUE,
    FALSE,
    TRUE,
    FALSE,
    TRUE,
    '["required","min_selections","max_selections"]'::jsonb,
    '{"candidate_roster":true,"selection_limits":true,"awareness_filter":true}'::jsonb,
    '{"min_selections":1,"max_selections":1,"include_undecided":true}'::jsonb,
    '{"answer_schema":"eios.ballot-selection.v1","candidate_source":"QUESTION_CHOICE_LIST"}'::jsonb,
    27,
    'Vote',
    'Use for single-seat and multi-seat ballot tests. Limits and candidate visibility are read from question metadata.',
    TRUE,
    TRUE
)
ON CONFLICT (type_code) DO UPDATE SET
    type_name = EXCLUDED.type_name,
    description = EXCLUDED.description,
    category_group = EXCLUDED.category_group,
    renderer_component = EXCLUDED.renderer_component,
    editor_component = EXCLUDED.editor_component,
    preview_component = EXCLUDED.preview_component,
    response_data_type = EXCLUDED.response_data_type,
    supports_options = EXCLUDED.supports_options,
    supports_validation = EXCLUDED.supports_validation,
    supports_logic = EXCLUDED.supports_logic,
    supports_repeat = EXCLUDED.supports_repeat,
    supports_matrix = EXCLUDED.supports_matrix,
    supports_offline = EXCLUDED.supports_offline,
    allowed_validation_rules = EXCLUDED.allowed_validation_rules,
    allowed_option_settings = EXCLUDED.allowed_option_settings,
    default_settings_json = EXCLUDED.default_settings_json,
    renderer_metadata_json = EXCLUDED.renderer_metadata_json,
    display_order = EXCLUDED.display_order,
    icon_name = EXCLUDED.icon_name,
    help_text = EXCLUDED.help_text,
    is_active = TRUE;
